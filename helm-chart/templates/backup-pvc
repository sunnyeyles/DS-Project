apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
  - {{ .Values.backupVolume.access }}
  resources:
    requests:
      storage: {{ .Values.backupVolume.storage }}
